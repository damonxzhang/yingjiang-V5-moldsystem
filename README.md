# SmartMold 模具管理系统 - V 5.1.20260716.030

## V 5.1.20260716.030 更新要点
- **设备看板（MachineDashboard）交互优化**：
  - **模台点击直接定位**：点击设备卡片中的某个特定模台（如 BMD-13 的 P2 位置 QF22）进入指挥中心时，将直接默认选中该模台，而非默认选中第一个模台（P1）。
  - **独立点击事件**：每个模具位置区域新增独立的点击事件，使用 `e.stopPropagation()` 阻止冒泡，确保点击模台时不会触发机台卡片的整体点击事件。
  - **可选参数支持**：`handleMachineClick` 函数新增可选参数 `defaultSlot`，支持外部传入默认选中的模台位置。
  - **用户体验提升**：避免用户点击特定模台后误操作到第一个模台的问题，提升操作准确性。

# SmartMold 模具管理系统 - V 5.1.20260701.029

## V 5.1.20260701.029 更新要点
- **APP首页（Home）重构**：
  - 新增**待办清单**板块，与设备看板数据同步，自动获取 BMD-01 的待办任务。
  - 3种不同类型待办任务以不同卡片样式展示：MMS保养（工具图标/琥珀色）、模具下架（向下箭头/红色+「待拆下」徽标）、维修（扳手图标/蓝色）。
  - 移除概览统计卡片（在线模具/待处理任务）。
  - 快捷入口按钮缩小为单行4列布局。
  - MMS保养卡片点击直接跳转至保养流程的**扫描模具二维码**页面（`maintenance_scan`）。
  - 移除卡片上的 `#2001-N` 编号标识。

- **保养流程（MaintenanceFlow）大幅简化**：
  - **删除模具去向选择页（DESTINATION）**：点击"保养结束"后直接跳转至"模具安装扫码"（FINAL），不再询问模具去向。
  - **删除半年 PM 逻辑校验提示**：FINAL页面中的校验提示区块已移除。
  - 新增 `initialStep` prop 支持外部指定初始步骤（`maintenance_scan` 路由直接跳转到 SCAN 步骤）。

- **设备看板（MachineDashboard）优化**：
  - 3套模具合并为1条"MMS半年保养"待办，确认弹窗中以3列网格展示3套模具。
  - 提示文字改为"计划开始时间默认是MMS系统提供的有效开始时间"。
  - 待办计数从3改回1。

- **模具管理筛选增强**：
  - 新增4个筛选字段：负责人、产品类型、位置、实时SHOT COUNT（区间范围）。

- **新增 `maintenance_scan` 路由**：
  - AppLayout 支持 `'maintenance_scan'` 屏幕标识，直接以 SCAN 步骤启动 MaintenanceFlow。
  - Home 组件和 HomeProps 类型同步更新。

# SmartMold 模具管理系统 - V 5.1.20260701.028

# SmartMold 模具管理系统 - V 5.1.20260623.027

# SmartMold 模具管理系统 - V 5.1.20260623.026
- **设备看板 - 任务状态彩色横条（P1/P2/P3）**：
  - **卡片级状态指示**：在每个设备卡片的 P1/P2/P3 槽位标签右侧，新增彩色横条，直观反映每个槽位的任务状态。
  - **详情弹窗侧边栏同步**：设备详情弹窗的槽位选择器（P1/P2/P3 按钮）中也同步添加了彩色横条。
  - **颜色映射**：
    - 🟤 物色：空槽位 / 无模具
    - 🔵 蓝色：正常生产中
    - 🔴 红色：超期 / 离线
    - 🟡 黄色：即将保养
    - 🟣 紫色：保养中

- **保养执行流程（MaintenanceFlow）- BUYOFF 信息录入**：
  - **新增 BUYOFF 步骤**：在 FINAL 页面点击"完成并关闭流程"后，不再直接返回，而是跳转至 BUYOFF 信息录入页面。
  - **完整表单**：包含 Process ID、User ID、Nick Name、Station 等只读字段，以及 Detail Reason、Material Type、Is Custom 等输入字段。
  - **凡工接口模拟**：点击"执行 BUYOFF 状态确认"按钮模拟调用凡工接口，1.5秒后返回 Reason/Method/Standard/Status 数据。
  - **接口返回验证卡片**：展示接口返回的所有数据，并提供"完成流程并关闭"按钮。

- **当前位置信息增强**：
  - MaintenanceFlow 的取模位置页面，"当前位置"显示从"位置: BMD-01"改为"机台号：BMD-01、模台号：P1"，同时展示机台号和模台编号。

- **其他 UI 优化**：
  - 修复 MaintenanceFlow 中 `handleSource` 函数未定义的问题，确保扫码后流程正常跳转。

# SmartMold 模具管理系统 - V 5.1.20260623.025

## V 5.1.20260623.025 更新要点
- **设备看板 - 保养确认流程重构**：
  - **保养确认二级弹窗**：系统定时任务生成的保养任务，按钮改为琥珀色"确认信息"，点击弹出二级确认页面。
  - **有效时间范围展示**：弹窗中展示来自第三方系统的保养有效时间范围（红色边框突出显示），以及当前系统时间。
  - **自定义保养时间**：用户可自行设定保养开始/结束时间，系统校验是否在有效范围内。
  - **"是否需要借机"单选按钮**：在创建保养任务弹窗中增加"是否需要借机"选项。
  - **DateTimeDropdownPicker 下拉选择器**：将原生 `datetime-local` 输入框替换为自定义年/月/日/时/分下拉选择组件，提供更好的交互体验。
  - **下拉框稳定性修复**：将 `DateTimeDropdownPicker` 从 `MachineDashboard` 组件内部移至外部独立定义，解决因父组件每秒刷新导致 `isOpen` 状态丢失、下拉框闪烁关闭的问题。
  - **Mock 数据回退策略**：`fetchMachineTodoList` 直接返回 mock 数据（跳过 API 调用），确保 BMD-01 设备始终显示3条保养待办测试数据。
  - **待办数字同步**：BMD-01 的 `pending_todos_count` 硬编码为3，与 mock 数据保持一致。

- **保养执行流程（MaintenanceFlow）深度改造**：
  - **待保养任务列表数据更新**：将列表中的模具数据从 QF16、MD-2024-001 替换为 TY06、TY12、TY02 三套模具。
  - **扫描二维码页面增强**：
    - 新增红色提示："扫描模具设备成功后即正式开始保养，后台的模具、设备、任务三个边框会变成红色"。
    - 新增红色提示："扫码时系统会校验是否符合预设的保养时间范围"。
    - 新增"模台编号"扫描输入框，支持扫码或手动输入。
  - **移除设备生产能力判定步骤**：简化流程，扫码后直接进入保养中页面，跳过了"设备生产能力判定"的中间步骤。
  - **完成流程优化**：点击"完成并关闭流程"按钮不再弹出提示弹窗，直接返回上一页。

- **Mock 测试数据扩展**：
  - **新增三套模具测试数据**：在 `MOCK_MOLDS` 中添加 TY06（注塑模）、TY12（压铸模）、TY02（注塑模），均位于 BMD-01 设备。
  - **待办保养工单更新**：将 `MOCK_WORK_ORDERS` 中的 PENDING 工单从 QF16/MD-2024-001 更新为 TY06/TY12/TY02，并新增第三条工单。

- **取模位置逻辑统一**：
  - **MaintenanceFlow.tsx** 和 **RepairFlow.tsx**：取模位置始终显示为设备信息（`sourceType` 固定为 `'MACHINE'`），不再提供从模具柜取模的选项。

# SmartMold 模具管理系统 - 待沟通与待办事项
- [ ] **接口 UserId 传参规范化**：当前多个接口调用中，`user_id` 字段采用硬编码默认值 `"1"`。需确认后续是否需要根据实际登录用户动态传参。
- [x] **免登录看板权限控制**：对于无需登录即可访问的看板页面，所有的操作性按钮（如安装、卸载、保养等）应统一置灰不可点击，并增加“请先登录”的交互提示。
- [x] **待处理事项角标实现**：已在设备生产看板中实现了待办任务角标显示，通过 `pending_tasks` 字段实时反馈。
- [ ] **大小材料逻辑隔离**：需确保登录后能准确判断大小材料身份，不同页面的操作逻辑（如安装、保养等）应严格区分，避免功能混淆。

# SmartMold 模具管理系统 - V 5.1.20260404.024

## V 5.1.20260404.024 更新要点
- **设备看板 UI 体验深度优化**：
  - **待办任务角标回归与增强**：
    - **逻辑修正**：修复了之前因字段引用错误导致的待办任务角标不显示问题，现已统一使用 API 返回的 `pending_tasks` 核心字段。
    - **位置优化**：为避免遮挡模具槽位（特别是 P4 位置）的寿命进度条，将角标从右下角移至机台标题栏（机台编号右侧）。
    - **视觉强化**：新增了 `fa-list-check` 图标，采用靛蓝色调与小圆角矩形设计，显著提升了辨识度与点击感。
  - **机台配置模块细节打磨**：
    - **查询功能增强**：在“机台模台配置”列表页新增了“机台编号”搜索功能，并配备了显式的“查询”按钮，提升大批量配置下的检索效率。
    - **操作权限收紧**：根据业务规范，从配置主列表页移除了“新增配置”和“删除”按钮，将配置管理聚焦于现有槽位的启用与别名修改。

# SmartMold 模具管理系统 - V 5.1.20260404.023

## V 5.1.20260404.023 更新要点
- **功能新增 - 机台模台配置模块**：
  - **模块入口**：在 Web 后台管理系统中新增了“机台模台配置”功能模块。
  - **槽位管理**：支持为每个机台单独配置其可用的模台槽位（P1、P2、P3、P4），包括启用/禁用状态。
  - **显示名称定制**：支持为每个槽位设置自定义的显示名称或别名，增强看板显示的灵活性。
  - **权限集成**：新增了 `machine:config` 权限，并已集成至 RBAC 体系中。
  - **UI 优化**：采用了全新的卡片式布局和交互设计，提升了配置操作的便捷性。

# SmartMold 模具管理系统 - V 5.1.20260404.022

## V 5.1.20260404.022 更新要点
- **系统稳定性与接口规范化**：
  - **修复看板渲染错误**：解决了 `MachineDashboard.tsx` 中 `productOptions.map is not a function` 的 TypeError 报错。通过在 UI 层增加 `Array.isArray` 防御性检查，确保了高密度数据下的渲染稳定性。
  - **接口请求方式校正**：将 `fetchMachineCodes` 接口的请求方式从 `GET` 修正为 `POST`，并补全了 `user_id` 与 `department` 必要参数，实现了与后端 `/api/admin/dashboard/machines/codes` 规范的精准对接。
  - **异常处理增强**：在 `DashboardService` 中完善了针对非数组响应的兼容逻辑，确保在接口异常或返回非预期格式时系统仍能保持运行而不崩溃。

# SmartMold 模具管理系统 - V 5.1.20260402.021

## V 5.1.20260402.021 更新要点
- **访客模式权限控制完善**：
  - **登录提示弹窗**：访客点击操作按钮（创建保养/报修任务、安装/卸载模具、模具停用）时，弹出登录提示弹窗，引导用户登录。
  - **按钮交互优化**：访客模式下按钮保持原功能名称，样式置灰，点击后显示"需要登录"提示弹窗，提供"前往登录"和"取消"选项。
  - **大小材料切换优化**：访客模式下点击大材料/小材料切换按钮直接调用接口刷新数据，无需跳转页面。
  - **统一认证服务**：所有页面统一使用 `AuthService.getStoredAuth()` 获取认证数据，确保数据一致性。
  - **返回登录功能**：访客模式下显示"返回登录"按钮，点击后调用 `handleLogout` 清除访客状态并返回登录页。
  - **设计文档更新**：完善了《免登录看板权限控制设计文档》，包含完整的实现细节和代码示例。

# SmartMold 模具管理系统 - V 5.1.20260401.020

## V 5.1.20260401.020 更新要点
- **模具管理功能深度增强**：
  - **“绑定机台/模台”双选功能上线**：在模具档案详情页面的“内部配件清单”模块上方，新增了“绑定机台”与“绑定模台”两个多选下拉框，支持并排显示与独立操作。
  - **精准接口对接**：
    - 机台列表获取接口更新为 `/api/admin/dashboard/machines/codes`。
    - 模台列表获取接口更新为 `/api/admin/dashboard/mold-tables/codes`。
  - **数据闭环同步**：完善了模具保存逻辑，选中的机台编号通过 `bound_machines` 提交，模台编号通过 `bound_mold_tables` 同步持久化至后端。
  - **交互体验优化**：
    - **并排布局**：采用 `grid-cols-2` 布局，并使用靛蓝 (Indigo) 与翠绿 (Emerald) 色调区分机台与模台。
    - **预加载机制**：机台与模台列表在页面初始化时自动异步加载。
    - **状态自动重置**：切换不同模具或新增模具时，自动清空及重置所有已选状态。

# SmartMold 模具管理系统 - V 5.1.20260331.019

## V 5.1.20260331.019 更新要点
- **数据规范化与类型安全升级**：
  - **模具分类字段集成**：在 `DashboardMold`、`InventoryMold` 和 `CurrentMoldDetail` 接口中全面引入 `mold_category` 字段。
  - **UI 逻辑回退机制**：设备详情弹窗中的“模具型号”现在优先显示 `mold_category` 字段。同时在 `createMoldData` 中实现了完善的 `type` 回退逻辑，确保了与历史数据的向下兼容性。
  - **代码健壮性提升**：通过更新接口定义，消除了 UI 层面的类型断言，增强了代码的可维护性。

# SmartMold 模具管理系统 - V 5.1.20260331.018

## V 5.1.20260331.018 更新要点
- **看板 UI 与数据逻辑深度优化**：
  - **模具标签内容升级**：看板模具标签现在优先显示接口返回的 `short_name`。增加了完善的防御性回退逻辑，若 `short_name` 缺失则自动显示 `mold_code` 或 `mold_id`。
  - **视觉密度调整**：将看板中模具标签及预警图标的字体字号从 `11px` 统一调小至 `9px`，显著提升了高密度机台排布下的可读性。
  - **实时数据同步**：机台卡片右上角的产品信息现在直接展示接口返回的 `part_no` 字段值，移除了之前的随机模拟数据。
- **“安装模具”流程细节打磨**：
  - **接口路径精准对接**：根据最新后端规范，将模具安装接口地址更新为 `/api/admin/machine/install`。
  - **传参机制优化**：为 `installMold` 和 `fetchInventoryMolds` 接口实现了 `user_id` 默认值（固定为 `"1"`）逻辑，确保在未显式传参时仍能满足后端 Request Body 的必填项要求。
  - **安装成功状态同步**：修复了安装成功弹窗中模具编号显示为 `EMPTY` 的问题。现在通过 `selectedMoldInfo` 状态即时捕获并展示新安装模具的 `short_name` 或 `mold_code`。

# SmartMold 模具管理系统 - V 5.1.20260331.017

## V 5.1.20260331.017 更新要点
- **“安装模具”功能全流程闭环**：
  - **库存模具选择弹窗**：重构了安装模具的交互逻辑。点击按钮后不再直接操作，而是弹出“库存模具清单”窗口，支持按模具编号/名称实时搜索过滤。
  - **API 接口深度集成**：
    - **自动关联机台**：`fetchInventoryMolds` 接口现在自动从 `/api/admin/dashboard/machines/status` 获取当前选中的 `machine_id` 进行联动。
    - **规范化安装请求**：实现了 `installMold` 接口，严格遵循 `{ action: "INSTALL", machine_id, slot, mold_id, user_id }` 的标准 JSON 格式。
  - **数据兼容性增强**：在所有 API 调用中增加了强制类型转换，确保 `machine_id`、`mold_id` 等字段以字符串形式发送，提升了与后端接口的对接稳定性。
- **系统监控与调试优化**：
  - **全链路日志记录**：为模具安装、库存查询等核心操作增加了详细的控制台日志记录（Request/Response），极大提升了现场部署时的排错效率。
  - **异常处理机制**：完善了弹窗内的加载状态（Loading）及错误反馈逻辑，确保用户在接口异常时能获得清晰的提示。

# SmartMold 模具管理系统 - V 5.1.20260331.016

## V 5.1.20260331.016 更新要点
- **看板视觉逻辑优化**：
  - **空槽位自动识别**：针对接口返回 `status: 'EMPTY'` 的位置，实现自动显示灰色边框并隐藏内部文字，使看板布局更清晰。
  - **状态颜色统一**：更新了状态标签（Span）的显示逻辑，`EMPTY` 状态统一显示为灰色，非空状态显示为绿色。
- **访客模式深度增强**：
  - **会话持久化修复**：修复了访客模式下因缺少 Token 导致的接口调用失败问题。现在模拟访客身份会自动同步至 LocalStorage 并设置 30 天有效期。
  - **参数映射优化**：完善了 `dept` 参数的映射逻辑（`big` -> 大材料, `small` -> 小材料, `dept=` -> 全部部门）。
- **权限与安全性锁定**：
  - **操作按钮锁定**：在设备看板详情中，针对访客禁用了“创建保养/报修任务”、“安装/卸载模具”及“模具停用”等所有写操作按钮，并增加了“仅限登录用户操作”的视觉提示。
  - **菜单深度隔离**：开启了侧边栏菜单过滤，访客仅能看到“设备生产看板”等监控类页面，管理类后台页面已完全隐藏。
- **系统稳定性**：
  - **逻辑修复**：修复了 `AdminLayout.tsx` 中菜单过滤逻辑导致的 `ReferenceError` 报错。

# SmartMold 模具管理系统 - V 5.1.20260320.015

SmartMold 是一款专为现代化工厂设计的智能化模具管理与维护平台。系统通过实时监控、数据分析和预测算法，全方位提升模具使用寿命，优化备件库存，确保生产流程的连续性与高效性。

## V 5.1.20260320.015 更新要点
- **项目排期更新**：根据最新沟通结果，明确了接口提供时间节点。
  - **网站接口**：预计于 2026-03-24 提供。
  - **APP 接口**：预计于 2026-03-25 提供。
- **进度管理增强**：同步更新了 [PROJECT_SCHEDULE.md](file:///d:/其他项目/NXP/归档/张英江/模具管理系统/设计方案/原型方案-第六版-正式启动/PROJECT_SCHEDULE.md) 的核心里程碑与详细执行计划，确保项目按时交付。

## V 5.1.20260319.014 更新要点
- **数据库架构优化**：为 `moldsys` 数据库中的所有 23 张表补齐了 `created_at` (创建时间) 和 `updated_at` (修改时间) 字段。
- **数据完整性增强**：所有新增时间戳字段均设置了 `GETDATE()` 默认值，并同步更新了数据库字段注释（Extended Properties），提升了系统的可维护性与审计追踪能力。
- **文档同步更新**：完善了 [DATABASE_SCHEMA_DOC.md](file:///d:/其他项目/NXP/归档/张英江/模具管理系统/设计方案/原型方案-第六版-正式启动/DATABASE_SCHEMA_DOC.md) 详细设计文档。

## V 5.1.20260319.013 更新要点
- **稳定性修复**：修复了 `App.tsx` 中由于标签嵌套错误导致的编译报错。
- **界面微调**：优化了访客模式下“返回登录”按钮的位置，集成进顶部中央切换器。

## V 5.1.20260319.012 更新要点
- **界面布局重排**：
  - **开发模式切换器重排**：将顶部的“APP执行/Web管理”切换器及用户信息标签移至屏幕顶部中央位置。
  - **消除遮挡问题**：解决了原切换器位于右上角时遮挡后台管理系统 Header 中“刷新”和“退出登录”按钮的问题。
  - **视觉优化**：切换器采用了半透明毛玻璃效果及阴影设计，既不影响主界面操作，又保持了调试工具的便捷性。

## V 5.1.20260319.011 更新要点
- **界面体验优化**：
  - **显眼登出入口**：在后台管理系统的顶部导航栏（Header）中新增了“退出登录”按钮，采用醒目的红色设计，方便用户随时安全退出。
  - **多位置支持**：保留侧边栏底部的登出按钮，用户可根据操作习惯在多处位置执行退出操作。

## V 5.1.20260319.010 更新要点
- **访客模式优化**：
  - **修复跳转逻辑**：修复了访客进入后自动跳转到模具台账的问题，现在默认保留在“设备生产看板”页面。
  - **数据精准过滤**：看板（设备看板与数据仪表盘）现在会根据 URL 参数（`?dept=big/small`）自动过滤展示对应部门的数据。
  - **权限深度锁定**：访客模式下，侧边栏仅展示“看板”类页面，其他后台管理页面（如台账、维修、用户管理等）已被深度隐藏，确保数据安全。

## V 5.1.20260319.009 更新要点
- **访问模式增强**：
  - **访客免登录模式**：支持通过特定 URL 参数（`?guest=true&dept=big/small`）直接访问大材料或小材料看板，无需登录账号。
  - **登录页快速链接**：在登录界面新增“快速看板访问”入口，方便外部人员快速查看生产数据。
  - **访客权限控制**：访客模式下系统自动锁定对应部门视图，并隐藏 APP 执行端切换功能，确保数据安全。

## V 5.1.20260319.008 更新要点
- **功能精简**：
  - **移除 Windows 登录功能**：根据需求去除了登录界面的 Windows 账号自动登录按钮及相关模拟逻辑。
  - **恢复纯净登录 UI**：登录页面回归标准用户名/密码验证模式。

## V 5.1.20260319.007 更新要点
- **身份验证 UI 深度定制**：
  - **动态识别状态显示**：在 Windows 登录按钮下新增了 `DETECTED` 状态标签，实时展示当前操作系统的真实登录用户（`laptop-gmj4rotp\user`）。
  - **UI 细节打磨**：优化了备注信息的排版与交互动效，使其更符合工业化系统的高级感。

## V 5.1.20260319.006 更新要点
- **身份验证功能深化**：
  - **真实 Windows 用户名集成**：系统现在已集成并显示当前操作系统的真实登录用户名（`laptop-gmj4rotp\user`），而非演示用的模拟数据。
  - **登录按钮备注强化**：在 Windows 账号自动登录按钮下方增加了当前用户名的备注显示，增强了登录前的身份识别。

## V 5.1.20260319.005 更新要点
- **登录体验优化**：
  - **Windows 账号备注**：在 Windows 自动登录按钮下方增加了当前用户名的备注显示（如 `当前用户: NXP\Admin`），增强了登录前的感知度。
  - **交互细节调整**：优化了 Windows 登录按钮的布局和加载动画。

## V 5.1.20260319.004 更新要点
- **身份验证功能增强**：
  - **Windows 账号登录**：在登录页面新增“Windows 账号自动登录”功能。
  - **自动识别逻辑**：模拟自动获取 Windows 当前登录用户名（如 `NXP\Admin`），并实现一键快捷登录，提升现场人员的使用便捷性。

## V 5.1.20260319.003 更新要点
- **模具转换流程优化**：
  - **按钮位置互换**：在 APP 端模具转换页面中，将“模具安装”与“模具拆下”按钮位置互换，提升操作直观性。
  - **图标语义增强**：统一规范了箭头指向，“模具安装”使用向上箭头 (fa-arrow-up)，“模具拆下”使用向下箭头 (fa-arrow-down)。
  - **待处理清单同步**：同步更新了任务列表中的状态图标，确保与操作按钮保持一致。
- **接口文档完善**：
  - **新增冲次原始数据接口**：在 WEB_ADMIN_INTERFACE_SUMMARY.md 中新增了 `/api/admin/monitor/shot-logs` 接口，支持按模具 ID、机台号及日期范围查询原始冲次日志。

## V 5.1.20260305.002 更新要点
- **APP 个人中心功能上线**：
  - **个人信息查看**：在 APP 端集成了“我的”页面，支持查看当前登录用户的姓名、角色、所属部门、邮箱及最后登录时间。
  - **退出登录逻辑**：实现了完整的退出登录流程，支持二次确认并返回登录模拟页面，增强了系统的安全性与交互闭环。
- **APP 首页交互精简**：
  - **移除冗余区块**：从 APP 首页移除了“寿命极限预警”及“最近活动”区块，使移动端界面更加聚焦于核心快捷入口。
  - **UI 纯净化**：简化了首页布局，提升了操作员在现场环境下的响应速度与视觉清晰度。
- **管理后台导航优化**：
  - **模块状态标识**：将侧边栏“任务中心”重命名为“【任务中心（待定）】”，明确标识该模块目前的待定状态。
- **README 维护**：同步更新了核心功能模块描述，新增了“模具配件绑定”功能说明。

## V 5.1.20260305.001 更新要点
- **维保选项管理功能上线**：
  - **保养选项 CRUD**：新增后台“保养选项管理”页面，支持对模具保养的标准作业内容进行增删改查及分类维护。
  - **维修选项 CRUD**：新增后台“维修选项管理”页面，支持对模具维修的标准故障处理内容进行增删改查及分类维护。
  - **导航集成**：在管理后台侧边栏集成了新增的维保选项管理入口，并适配了相应的权限控制逻辑。
- **README 与文档同步**：更新了系统功能描述文档，并将最新变更同步至 GitHub/Gitee。

## V 5.1.20260226.001 更新要点
- **用户管理功能修复与增强**：
  - **新增用户修复**：修复了管理后台“新增用户”按钮失效的问题，完善了用户创建流程。
  - **CRUD 功能完善**：支持用户信息的编辑与删除，操作反馈更加直观。
- **角色权限体系重构**：
  - **角色标准化**：根据业务需求，将原有的角色体系精简并标准化为“管理员、大材料工程师、小材料工程师、带班、操作员”五大类。
  - **全平台适配**：更新了 Web 后台菜单权限、APP 首页操作权限以及全平台各处角色标签的映射显示，确保 RBAC（基于角色的访问控制）的一致性。
  - **权限细化**：为“带班”角色赋予了任务中心确认及维修保养执行的查看权限，增强了现场管理能力。

## V 5.1.20260213.002 更新要点
- **库存模具状态增强**：
  - **动态位置显示**：库存列表中，闲置模具显示“柜号”，使用中模具显示“设备号”，维修/保养模具隐藏位置信息。
  - **使用数据可视化**：库存模具卡片新增实时 SHOT COUNT 及健康度进度条（支持 70%/90% 阈值变色）。
  - **UI 规范化**：将“生产中”状态统一更名为“使用中”，颜色调整为黄色，并将“机台”前缀更新为“设备号”。
- **模具台账功能升级**：
  - **新增 SHOT 上限列**：在模具管理表格中增加“SHOT 上限”列，支持与实时冲次实时对比。
  - **新增“所在设备”列**：实时展示模具当前绑定的设备编号及运行状态（生产中/离线）。
  - **审计模式适配**：确保新增列在审计模式下按需隐藏。

## V 5.1.20260212.004 更新要点
- **模具管理深度定制**：在模具台账中新增了模具简名 (short_name)、模具厚度 (thickness)、模具分类 (mold_category)、产品类型 (product_type) 四个专业字段。
- **多部门管理架构**：将“模具台账”与“备件管理”模块正式拆分为“大材料”与“小材料”两个独立入口，支持数据的逻辑隔离。
- **模具停用功能**：在后台管理及设备指挥中心均增加了“模具停用”按钮，点击并确认后，该模具将标记为已停用，并从所有生产看板及安装候选中自动过滤。
- **详情页信息强化**：在指挥中心模具状态面板中新增了“模具全称”与“模具简称”显示，优化了工程人员核对信息的效率。
- **视觉交互优化**：
  - 从机台详情页中移除了冗余的“生产进度条”，使界面更聚焦于模具健康度。
  - 在待办清单中，将“去处理”升级为“确认完成”按钮，提供中划线及背景变色的视觉完成反馈。
- **数据一致性修复**：修复了备件管理模块中 MOCK 数据引用的拼写错误，确保部门过滤逻辑准确。

## V 5.1.20260212.003 更新要点
- **待办事项完成确认**：在待办清单弹窗中，将“去处理”按钮升级为“确认完成”逻辑。用户点击后，任务将标记为已处理状态（文字中划线、状态变更、背景转绿），再次点击可撤销。
- **自助模台管理**：在“生产就绪清单”详情区域新增了“自助管理”功能。用户可以实时添加新工位（输入 Mold ID，系统自动分配 P1-P4）或删除现有工位，界面布局和表格统计将随之动态更新。
- **进度条百分比**：在机台看板的模具寿命进度条下方新增了具体的百分比数值显示，并根据进度自动切换颜色（绿色/黄色/红色），增强了数据直观性。
- **产品配置动态化**：在“生产就绪清单”中实现了产品工位的动态配置。支持根据不同产品的工艺需求，灵活配置 2、3 或 4 个生产工位（P1-P4）。
- **待办事项角标**：在机台看板卡片右下角新增了待办事项统计角标。点击角标可弹出“待办清单”对话框，集中查看该机台下所有模具的保养、维修、更换及验证任务。
- **模具配置动态化**：实现了机台模具数量的动态加载，支持单机挂载 2、3 或 4 个模具，看板布局随模具数量自动调整（2列/3列/4列）。
- **界面精简**：从机台看板中移除了生产进度条（Progress），使界面更加聚焦于模具健康状态监控。
- **筛选增强**：新增“显示异常生产设备”复选框，一键定位需要关注的设备。
- **视觉优化**：加粗了模具寿命进度条（60%变黄，90%变红并增加发光效果），加粗了看板卡片边框。
- **交互说明**：完善了看板说明弹窗，增加了所有进度条的详细定义说明。
- **边框修复**：解决了筛选后看板卡片边框厚度不一致的问题。

## 待办事项 (TODO)
- [ ] **图片查看功能**：在后台管理系统中支持查看保养和维修任务上传的现场图片。

## 技术特性
- **技术栈**：React + TypeScript + Tailwind CSS + Vite。
- **现代化 UI**：专为工业大屏设计的深色主题（Dark Mode），高对比度视觉反馈。
- **响应式设计**：适配管理后台与大屏显示。

## 快速开始

### 开发环境配置
1. **安装依赖**：
   ```bash
   npm install
   ```
2. **启动项目**：
   ```bash
   npm run dev
   ```
3. **访问地址**：默认访问 [http://localhost:3000](http://localhost:3000)（如端口占用会自动切换）。


## 登录认证系统

### 功能说明

SmartMold 系统现已集成完整的用户登录认证功能，支持：

- ✅ 用户名/密码登录
- ✅ 会话状态管理（刷新页面保持登录）
- ✅ 安全登出功能
- ✅ 基于角色的权限控制
- ✅ 表单验证和错误提示
- ✅ 加载状态显示

### 测试账号

系统提供以下测试账号供开发和演示使用：

| 用户名 | 密码 | 角色 | 部门 |
|--------|------|------|------|
| admin | admin123 | 系统管理员 | 管理部门 |
| engineer_big | eng123 | 大材料工程师 | 大材料 |
| engineer_small | eng123 | 小材料工程师 | 小材料 |
| leader | lead123 | 带班长 | 生产部门 |
| operator | op123 | 操作员 | 生产部门 |

### 使用说明

1. **首次访问**：打开应用时会自动显示登录页面
2. **输入凭证**：使用上述测试账号登录
3. **会话保持**：登录后刷新页面不需要重新登录
4. **切换角色**：如需测试不同角色权限，请先登出再使用其他账号登录
5. **安全登出**：点击侧边栏底部的"退出系统"按钮

### 技术实现

- **认证服务**：`services/authService.ts` - 处理登录、登出和会话管理
- **登录页面**：`pages/auth/LoginPage.tsx` - 独立的登录界面组件
- **会话存储**：使用 sessionStorage 存储认证令牌和用户信息
- **Mock 数据**：当前使用 mock 数据模拟登录，便于前端开发和测试
- **API 准备**：代码结构已为真实 API 集成做好准备，只需替换 mock 实现

### 安全特性

- 密码不在前端存储
- 会话数据在浏览器关闭后自动清除
- 无效会话自动重定向到登录页面
- 表单输入验证（防止空白输入）
- 错误消息友好提示

### 未来扩展

当后端登录 API 可用时，只需在 `services/authService.ts` 中将 mock 实现替换为真实的 API 调用即可。
